apiVersion: v1
kind: Service
metadata:
  name: ${{values.serviceName}}
  labels:
    metrics-scraper: prometheus
spec:
  selector:
    app: ${{values.serviceName}}
  ports:
    - name: http
      port: 8080
      targetPort: 8080